// prisma.config.ts — solo usado por Prisma Migrate (no por el cliente en runtime)
// Nota: este archivo es generado por Prisma 5+; los errores TS de "prisma/config" son
// de Prisma 7. Con Prisma 5 la URL se configura en .env via DATABASE_URL.
export {};
